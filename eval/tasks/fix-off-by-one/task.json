{
  "prompt": "There is a bug in src/lib.rs — the sum function has an off-by-one error. Fix it so all tests pass.",
  "verify": "cd src && rustc --edition 2021 --test lib.rs -o /tmp/nerv-eval-test && /tmp/nerv-eval-test",
  "max_turns": 10
}
