# todo-app

Minimal todo CLI for plan-mode testing.

```
python cli.py add "buy milk"
python cli.py add "write tests"
python cli.py list
python cli.py done 1
python cli.py remove 2
```

Tests: `pytest test_todo.py`
